# CMakeLists.txt to build programs/dgl/dgl-config

INCLUDE_DIRECTORIES( ${DGL_SOURCE_DIR}/include )
ADD_EXECUTABLE( dgl-config dgl-config.cpp )
INSTALL( TARGETS dgl-config RUNTIME DESTINATION bin PERMISSIONS OWNER_READ
		OWNER_WRITE OWNER_EXECUTE GROUP_READ GROUP_EXECUTE WORLD_READ
		WORLD_EXECUTE )

