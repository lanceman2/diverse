# CMakeLists.txt to build examples/quad

INCLUDE_DIRECTORIES( ${DTK_SOURCE_DIR}/include ${DGL_SOURCE_DIR}/include
		${VTK_INCLUDE_DIRS} ${PRODUCER_INCLUDE_DIR}
		${OPENTHREADS_INCLUDE_DIR} ${GLX_HEADER_PATH} )
ADD_EXECUTABLE( quad quad.cpp )
TARGET_LINK_LIBRARIES( quad dvtk )
SET_TARGET_PROPERTIES( quad PROPERTIES LINK_FLAGS
		"${DGL_LIB_FLAGS} ${DGL_VTK_LDFLAGS}" )

