# CMakeLists.txt to build programs/dgl/dgl-waitExec

INCLUDE_DIRECTORIES( ${DGL_SOURCE_DIR}/include ${DTK_INCLUDE}
		${PRODUCER_INCLUDE_DIR} ${OPENTHREADS_INCLUDE_DIR} )
ADD_EXECUTABLE( dgl-waitExec dgl-waitExec.cpp )
TARGET_LINK_LIBRARIES( dgl-waitExec dgl )
INSTALL( TARGETS dgl-waitExec RUNTIME DESTINATION bin PERMISSIONS OWNER_READ
		OWNER_WRITE OWNER_EXECUTE GROUP_READ GROUP_EXECUTE WORLD_READ
		WORLD_EXECUTE )

