# CMakeLists.txt to build examples/coinload

IF( WIN32 )
	ADD_DEFINITIONS( /DCOIN_DLL )
ENDIF( WIN32 )

INCLUDE_DIRECTORIES( ${DGL_SOURCE_DIR}/include ${DTK_INCLUDE}
		${GLX_HEADER_PATH} ${COIN_INCLUDE_DIR} ${PRODUCER_INCLUDE_DIR}
		${OPENTHREADS_INCLUDE_DIR} )
ADD_EXECUTABLE( coinload coinload.cpp )
TARGET_LINK_LIBRARIES( coinload dcoin )
SET_TARGET_PROPERTIES( coinload PROPERTIES LINK_FLAGS ${DGL_LIB_FLAGS} )

