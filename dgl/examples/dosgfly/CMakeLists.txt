# CMakeLists.txt to build examples/dosgfly

INCLUDE_DIRECTORIES( ${DGL_SOURCE_DIR}/include ${DTK_INCLUDE}
		${GLX_HEADER_PATH} ${OSG_INCLUDE_PATH} ${PRODUCER_INCLUDE_DIR}
		${OPENTHREADS_INCLUDE_DIR} )
ADD_EXECUTABLE( dosgfly dosgfly.cpp )
TARGET_LINK_LIBRARIES( dosgfly dosg )
SET_TARGET_PROPERTIES( dosgfly PROPERTIES LINK_FLAGS
		"${DGL_OSG_LDFLAGS} ${DGL_LIB_FLAGS}" )

